# 孙庆喆 8.6
## 8.6 Git 基础操作
+ [x] 作业1：第一次提交，利用vscode中git图形化操作完成了第一次提交代码
+ [x] 作业2：修改并提交打开`Core /Src/main.c`把
```#define BLINK_TIMES 3U```改成`5U`保存并提交“将闪烁次数调整为5”
+ [x] 作业3：分支与合并
  利用命令`git checkout -b feature-blink`创立新的名为`feature-blink`分支，在此分支里修改`blimk_led()`的循环次数，提交后然后用命令`git checkout main`切回到主分支，用命令`git log --oneline`查看提交历史，用命令`git diff main feature-blikn`查看两个分支中的代码差异然后用命令`git merge feature-blink`将`feature-blink`分支合并到`main`分支。
+ [x] 作业4：体验退回
  切换到`feature-blink`分支，使用命令`git reset --hard HEAD~1`退回丢掉最后一次提交。
+ [x] 作业5：远程仓库
  建立个人GitHub公开远程仓库，将代码上传到远程仓库中，练习git的各种基础操作。
+ [x] 作业6：Markdown学习记录
 将今天练过的Git操作写成Markdown文档并练习Markdown的常用语法。
  | 语法 |作用 |
  |--|--|
  |#|标题（最多六级）|
  |前后`|`行内代码`|
  |前后```|```代码段```|
  |前后*|*斜体*|
  |前后**|**加粗**|
  |前后~~|~~删除线~~|
  |前后==|==高亮==|
  |前<u>后<u/>|<u>下划线<u/>|
  |+或-|无序列表|
  |1. 2.|有序列表|
  |[ ] (URL连接)|超链接|
+  [x] C语言 + Vscode 工具链搭建
![alt text](<屏幕截图 2026-08-06 165931.png>)
## 8.6 git操作顺序
1. `init`→`add .`→`commit -m`
2. `checkout -b `→`checkout`→`log --oneline`→`diff`→`merge`
3. `reset --hard HEAD~1`
4. `add .`→`commit -m`→`push -u origin`
## git命令表格
 |命令|作用|什么时候用|
 |--|--|--|
 |`add .`|把当前目录加入暂存区|要把更改提交到存储库中时|
 |`init`|初始化当前项目|首次开启版本远程控制|
 |`commit -m`|将暂存区的修改提交到存储库中|要将修改内容保存到存储库|
 |`checkout -b `|创建新的分支|需要多次调试代码并且比较选出最优方案或许多人对代码操作|
 |`checkout`|切换分支|要从一个分支切换到另一个分支|
 |`log--oneline`|查看提交历史|需要具体查看提交的历史内容|
 |`diff`|代码内容对比|需要将两个分支中代码不同的地方找出比较|
 |`merge`|将分支合并|在分支中选择最优方案并把修改内容合并到主代码中|
 |`reset --hard HAED~1`|退回丢掉最后一次提交|需要去除最后一次提交内容|
 |`push -u origin`|将当前分支推送到存储库|创立了新的分支第一次推送入存储库|
 |`pull`|拉取存储库中的分支到当前代码|当前代码不是最新代码需要更新到与存储库中一致|
## 实际执行过的命令
```
git checkout main
git log --oneline
git diff main feature-blink
git merge feature-blink 
git reset --hard HEAD~1
```
[GitHub仓库的超链接](https://github.com/zhezhehanhan?tab=repositories)
# 孙庆喆 8.7
## 8.7编译烧录与工具链
+ [x] 作业1：确认工具链
1. 确认Keil MDK5、STM32H7器件支持包、ST-Link驱动都已安装；
2. 双击打开`MDK-ARM/keil_proj_demo.uvprojx`；
3. 按 `F7`编译，确认没有 error；
4. 连接ST-Link，按`F8`下载，复位后观察 LED 和蜂鸣器。
  观察到led灯从右向左依次闪烁四次，当四个led等均闪烁四次后蜂鸣器响一次，然后led灯和蜂鸣器再次重复上述过程
+ [x] 作业2：认识编译产物
 `F7`编译过程完成了对我们写的代码的预处理，输出纯净的c源码文本，然后将c源码文本翻译成CPU机器码，生成`.o`文件，链接器将`.o`文件，启用的文件和库文件整合，统一分配Flash和 RAM地址，合并所有代码数据，输出带有调试信息的`.axf`文件，由`.axf`文件导出方便烧录的`.hex`文件
  `F8`下载过程将`.axf`文件和`.hex`文件写进了芯片
  直接修改完代码后，之前生成的.o，.axf，.hex文件中的内容并没有改变，因此要再次编译下载才能实现真正意义上的修改代码。
+ [x] 作业3：配置任务
 +    任务1：把新组件添加进工程
      1. 在Keli里新建一个Group，命名为`user`
      2. 把`user/src/user_beep.c`添加进这个 Group
      3. 在`Options for Target->C/C++ ->Include Paths`里加上`..\user\inc`
      4. 打开`Core/Src/main.c`,把`//在这里替换成#include user_beep.h`这行改成`#include "user_beep.h"`
      5. 按`F7`编译，按`F8`下载，复位后听蜂鸣器是不是按“长-短-短”响三次
 +    任务2：移除已经在工程里的 hardware 文件，编译看报错
     1. 移除Include Paths里的`../hardware/inc`,报错如图：
      ![alt text](<屏幕截图 2026-08-07 104311.png>)
      找不到`buzzer.h`/`led.h`之类的头文件错误
    2.移除hardware Gourp里的`buzzer.c`和`led.c`编译后报错如图：
    ![alt text](<屏幕截图 2026-08-07 105032.png>)
    报 Undefined symbol，因为函数实现没了
    Include Paths 负责让编译器找到`.h`头文件，Group 里的`.c`负责提供函数实现
+ [x] 作业4：修改现象验证闭环
  把 DELAY_MS 从 250U 改成 500U，重新编译下载，观察速度变化
  可以明显观察到LED灯闪烁的时间间隔变长了，根据代码修改可以看出，闪烁时间间隔应该为之前闪烁时间间隔的2倍。
+ [x] 任务5：写一份工具链记录
  ### 对一些报错现象的分析与调整
  + ![alt text](<屏幕截图 2026-08-07 091705.png>)  
  在进行作业3任务1时，编译出现该报错，分析后得知，在Keli中进行Include Paths配置时，`../user/inc`该路径并没有正确添加进去，重新添加后报错消失，能够正确编译下载。
  ![alt text](<屏幕截图 2026-08-07 093655.png>)
  报错分析:在vscode中keil Assistant插件没有识别到有效的Keli工程项目，在Keli中可以正常编译，重启vscode和Keli后插件识别成功解决报错。